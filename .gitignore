.DS_Store
.lock
CLAUDE.md

# Internal development docs (design hand-offs, not user-facing role docs)
HANDOFF-cdn-transports.md

# Ansible local cache / runtime dir
.ansible/

# Offline test scratch (tests/run.sh cleans these up; belt and braces)
tests/.reg/
tests/.caddyfile.rendered
tests/.compose.rendered