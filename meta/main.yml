galaxy_info:
  author: lunarthegrey
  description: Deploys and CDN-fronts FreeSocks proxy nodes (Remnawave/Xray, optional Outline) and registers them with the FreeSocks Control Plane
  company: Unredacted Inc
  license: GPL-3.0-only
  # Needs community.docker >=3.4.0 (docker_compose_v2) + modern Jinja filters,
  # both of which require ansible-core 2.15+.
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions:
        - 12
        - 13
  galaxy_tags:
    - networking
    - security
    - vpn
    - cloudflare
dependencies: []
